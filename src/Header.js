export default function Header() {
  const headerStyle = {
    color: "orange",
    fontSize: "48px",
    textTransform: "uppercase",
  };

  return <h1 style={headerStyle}>Welcome to React Pizza Menu!</h1>;
}
